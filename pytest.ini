[pytest]
# Set Python path to parent directory (kiff-ai root) so 'backend.*' imports resolve correctly
pythonpath = ..

# Configure test discovery to look in the tests directory
testpaths = tests

# Optional: Use more verbose output
addopts = -v
